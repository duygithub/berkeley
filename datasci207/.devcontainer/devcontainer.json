{
    "name": "ML Quarto Environment",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter",
                "quarto.quarto"
            ]
        }
    },
    "remoteUser": "vscode",
    "updateContentCommand": "pip install --upgrade pip && pip install -r requirements.txt"
    // "postCreateCommand": "pip install --upgrade pip && pip install -r requirements.txt"
}
